#!/bin/bash

# overwrite this file in your child container
# it will be launched upon first start, to initialize the environment of a container
echo "I would now initialize the container... (e.g. create directories, symlinks, ...)" 
